from django.conf.urls.defaults import patterns, url, include

urlpatterns = patterns('home.views',
    #url(r'^$', 'index'),
    url(r’^$’,’ver_paginas’),
    url(r’^(?P<pagina_id>\d+)/$’,’ver_pagina’),
)
