parameters n; //devuelve 1 si n es primo, sino devuelve el primer divisor de ese número
   r=floor(sqrt(n));
   for(i=2; i<=r; i++) {
      if (n % i == 0) {
	     return i;
	  }
   }
return 1;

